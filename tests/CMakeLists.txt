function(add_unit_test name)
  add_executable(${name} ${name}.cpp)
  target_link_libraries(${name} PRIVATE umpnap_engine)
  target_include_directories(${name} PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
  add_test(NAME ${name} COMMAND ${name})
endfunction()

add_unit_test(smoke_test)
add_unit_test(linalg_test)
add_unit_test(fluid_test)
add_unit_test(network_test)
add_unit_test(registry_test)
add_unit_test(branchlaw_test)
add_unit_test(nodegraph_test)
add_unit_test(solver_test)
add_unit_test(validation_test)
add_unit_test(project_test)
